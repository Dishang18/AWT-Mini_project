<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Account</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-screen bg-zinc-900 text-white p-10">
        <h3 class="text-3xl mb-3">Create Account</h3>
        <form method="post" action="/register">
            <input class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-800" type="text" placeholder="username" name="username" required />
            <input class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-800" type="email" placeholder="email" name="email" required />
            <input class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-800" type="password" placeholder="password" name="password" required />
            <input class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-800" type="text" placeholder="age" name="age" required />
            <input class="px-5 py-2 rounded-md bg-blue-500" type="submit" value="Create User">
        </form>
        <p class="mt-5">Already have an account? <a href="/login" class="text-blue-400 underline">Login here</a>.</p>
    </div>
</body>
</html>
